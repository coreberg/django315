# Django 3.1.5(Python 3.9.1)

```
$ git config --global user.name "name"
$ git config --global user.email "email"

$ git init

$ git add -A
$ git status
$ git commit -m '-'
$ git branch -M main
$ git push -u origin main

$ git remote show origin
```